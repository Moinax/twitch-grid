{
  "name": "twitch-grid",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@11.25.0",
  "scripts": {
    "test": "playwright test",
    "check": "node --check app.js && node --check library.js && node --check api/search.js && node --check server.cjs",
    "dev": "node --watch --env-file-if-exists=.env.local server.cjs",
    "og:generate": "node scripts/render-share-image.cjs",
    "test:server": "node --test tests/search.test.cjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
